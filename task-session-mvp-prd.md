# 任务对话页面 MVP 产品需求文档

Status: design proposal
Author: product draft
Target: Hermes 项目协作空间 · 任务对话页面

## 1. 背景与目标

本文档定义任务对话页面：用户在某个专家 profile 下发起一次「任务式」对话（即一个 Hermes session），与专家进行多轮协作。对话过程中专家会调用工具、委派子代理、向用户提问、请求审批；页面需要承载这些过程中产生的全部内容。

**MVP 不依赖看板（Kanban）。** 产品层的「任务」在运行时就是 **Hermes session**；右侧列表来自 `session.list`，新建来自 `session.create`，切换来自 `session.resume`。看板 PRD（`project-kanban-mvp-prd.md`）是可选的上层编排：若后续接入，可通过 `kanban task.session_id` 关联，**不改变**本页 session 机制。

### 1.1 与看板 PRD 的关系（可选，非 MVP 阻塞）

- 看板 PRD 定义「项目 — 任务 — 专家」的**编排层**（board、dispatch、状态机）。
- 本 PRD 定义「用户与专家 profile 的对话工作台」，底层一律是 **SessionDB + session `cwd`**。
- 产品用语「一任务一 session」：一个任务 = 一个 Hermes session；切换任务 = 切换 session。
- 专家详情页「发起任务」、本页「+ 新建任务」均直接 **`session.create`**，**不**打开看板下发抽屉。
- 从看板跳转本页（若产品已接 Kanban）仅是多一个入口；本页仍可独立使用。

### 1.2 设计目标

1. **完整呈现对话过程中的内容**：用户消息、专家回复、工具调用、子代理委派、HITL 请求、错误等都要有对应的呈现方式。
2. **保留 Hermes 原生对话能力**：不复刻 hermes 已有的对话流，而是把 hermes 的事件流映射成 B 端用户能理解的 UI 内容块。
3. **B 端可控**：HITL 必须阻塞对话流，危险操作必须显式审批。
4. **MVP 聚焦核心路径**：高级能力（MoA、内嵌终端、动态模型切换）放后续版本；工作目录与 Hermes session `cwd` 对齐，产物落盘靠 SOUL 约定（见 §2.3）。

## 2. 产品定位

### 2.1 产品定义

任务对话页面是用户与单个专家 profile 就某个任务进行多轮对话的工作台。它不是自由群聊，而是一个有明确任务上下文、有 HITL 阻塞点、有工具调用可见性的结构化对话界面。

### 2.2 核心原则

1. **一任务一 session**
   - 产品「任务」= 一个 Hermes session（见 §8，`session.list` / `session.create`）。
   - 切换任务即 `session.resume`，对话上下文完全隔离。
   - 任务卡片副标题/预览来自 `session.list` 的 `preview` 或首条消息摘要。

2. **对话内容按事件流呈现**
   - 用户消息、专家回复、工具调用、子代理、HITL 都是对话流中的内容块。
   - 每个内容块对应 Hermes 一类事件，UI 形态由事件类型决定。

3. **HITL 阻塞对话流**
   - 澄清提问、危险操作审批必须阻塞专家执行。
   - 用户回应前专家不继续推进。
   - HITL 卡片在 UI 上强提醒，避免用户漏看。

4. **工具调用可见**
   - 每个工具调用独立成卡，按时间顺序排列。
   - 卡片展示工具名、参数预览、结果摘要、耗时。
   - 工具调用过程可折叠，默认展示摘要。

5. **子代理活动折叠呈现**
   - 主对话流只展示子代理「启动 + 完成 summary」两块卡片。
   - 中间活动详情折叠，点击可展开查看活动时间线。

6. **不做技能调用卡片**
   - Hermes 运行时没有「技能调用」独立事件。
   - profile 激活的技能列表在顶部状态栏展示，不在对话流里做卡片。

7. **工作目录 = 该任务的 workspace（对齐 Hermes session `cwd`）**
   - 每个任务 session 绑定一个**工作目录**（Hermes session `cwd`）：该任务的读写与 terminal 默认锚点，**即该任务的工作区**。
   - 每个 profile 另绑定**工作空间根**（`config.yaml` `terminal.cwd`）：专家级目录树根；**所有 session 均可访问整棵工作空间树**（浏览、读取任意文件）。
   - **默认关系（MVP）**：新建 session 时**默认工作目录 = 工作空间根**；用户可在创建前/创建后通过侧边栏或下拉改为子目录（如 `工位8/`），**不**自动创建 `tasks/<session-id>/`。
   - **产物约定**：任务产出须落在当前 session **工作目录**内；由 SOUL/skill 约定，MVP 不做写出工作目录外的硬拦截。
   - **不拆读写 root**：相对路径、terminal 默认相对 session `cwd`；工作目录为根时，相对路径即相对整棵工作空间；cwd 为子目录时 `@file:` 受 `allowed_root` 限制（见 §10.9.4），agent `read_file` 无此限制；v1.1 可扩 `@file:` 范围（见 §10.9.8）。

### 2.3 工作空间与工作目录

#### 2.3.1 概念定义

| 概念 | 产品含义 | Hermes / 存储映射 | 典型路径示例 |
|---|---|---|---|
| 工作空间根 | 专家 profile 绑定的文件树根；浏览、共享文件 | `config.yaml` `terminal.cwd`（profile 级） | `D:\workspace` |
| 工作目录 | **该任务 session 的工作区**（读 + 写 + terminal） | session 元数据 `cwd` | 默认 `D:\workspace\`（= 工作空间根）；用户可改为子目录 |
| 任务产物 | 本任务产出文件 | 须位于**工作目录**内 | `D:\workspace\报告.md` 或 `...\工位8\报告.md` |

推荐目录结构（**示例**，用户自行组织）：

```text
D:\workspace/                    ← 工作空间根（profile terminal.cwd）
├─ 工位8/                        ← 可选：用户将某 session 工作目录指到此
└─ AGENTS.md
```

#### 2.3.2 与 Hermes 对齐（MVP 默认行为）

session `cwd` **即**该任务 workspace，不做「读根 / 写根」拆分：

| 操作 | 行为 |
|---|---|
| Terminal 默认执行 | session 工作目录 |
| `read_file` / `write_file` / `patch`（相对路径） | 相对 session 工作目录 |
| `@file:` 引用（MVP） | 相对 session 工作目录展开，**`allowed_root` = session cwd**（见 §10.9）；cwd 为根时可直接 `@file:SOP.md`；cwd 为子目录时 `@file:` **无法**引用 cwd 外文件（`../`、绝对路径均被拦），需改用 agent `read_file` 工具或 v1.1 扩 `allowed_root` |
| 绝对路径 | OS 权限范围内均可访问（无产品层沙箱） |

**注意：** 对话记录、memory、skills 等仍在 profile 的 `HERMES_HOME`，不会写入工作目录。工作目录仅指 agent 通过 file/terminal 在磁盘上操作的 task 文件夹。

#### 2.3.4 读取工作空间内任意文件

MVP 默认 **session 工作目录 = 工作空间根**，相对路径即相对整棵工作空间。若用户将工作目录改为子目录（如 `工位8/`），**仍不限制**访问 profile 工作空间的其他部分：

| 能力 | 是否受 cwd 为子目录影响 | 说明 |
|---|---|---|
| 侧边栏浏览整棵工作空间树 | **否** | 树根始终是 profile `terminal.cwd`；任意 session 可展开任意子目录、其他任务目录等 |
| 预览工作空间内任意文件 | **否** | 侧边栏点击预览，与 session cwd 无关 |
| Agent `read_file` 读工作空间内文件 | 需显式路径 | 绝对路径直接可读（无 `allowed_root` 限制）；相对 cwd 用 `../SOP.md` |
| Agent 相对路径默认解析 | 相对 cwd | Hermes 原生；`data.csv` 指工作目录内文件，不会自动指到工作空间根 |
| `@file:`（MVP） | `allowed_root` = cwd | cwd 为子目录时**无法**引用 cwd 外文件（`../`、绝对路径均被拦，见 §10.9）；cwd 为根时整棵树可用 |
| `@file:` / `complete.path`（v1.1 可选） | `allowed_root` 扩至工作空间根 | cwd 为子目录时也可 `@file:SOP.md` |

读取方式示例：

| 方式 | 示例 | MVP 可行性 |
|---|---|---|
| 绝对路径（agent `read_file`） | `D:\workspace\SOP.md` | ✅ `read_file` 无 `allowed_root` 限制 |
| 相对工作目录（agent `read_file`） | `../SOP.md` | ✅ 仅软警告，不阻止 |
| `@file:`（cwd = 工作空间根） | `@file:SOP.md` | ✅ `allowed_root` = 根，整棵树可用 |
| `@file:`（cwd = 子目录） | `@file:../SOP.md` | ❌ `allowed_root` = cwd，`../` 越界被拦 |
| `@file:`（cwd = 子目录，绝对路径） | `@file:D:\workspace\SOP.md` | ❌ `allowed_root` 拦截绝对路径越界 |
| `@file:`（v1.1 扩 `allowed_root`） | `@file:SOP.md` | ✅ cwd 为子目录时也可用 |

#### 2.3.5 产物约定（MVP）

1. **专家 SOUL.md** 或 skill：明确「所有任务产物（报告、导出、生成文件）必须写在当前**工作目录**内」。
2. **新建任务**：`session.create` 时默认 `cwd` = profile 工作空间根（`terminal.cwd`），`explicit_cwd=true`；用户可在创建前选子目录，或创建后在侧边栏/下拉修改（见 §8.4）。
3. **UI**：状态栏/输入区展示当前**工作目录**（相对工作空间根的路径；为根时显示 `.` 或 `/`）；工作空间按钮打开**整棵**工作空间树。
4. **已知限制**：agent 仍可能用绝对路径写到工作目录外；v1.1 可选做轻量拦截（见 §14 v1.1）。

**v1.1 可选增强**：将 `@file:` / `complete.path` 的 `allowed_root` 从 session cwd 扩至 profile 工作空间根（见 §10.9.8），使 cwd 为子目录时 `@file:` 也能引用整棵工作空间。

## 3. MVP 范围

### 3.1 MVP 包含

- 顶部状态栏（profile、模型、工作目录、运行状态、工作空间按钮）
- 对话流区域
  - 用户消息（文本、图片、文件、选项回应、打断）
  - 专家消息（思考过程、流式回复、处理进度）
  - 工具调用卡片（开始、完成、错误）
  - 子代理卡片（启动、完成 summary、活动详情折叠）
  - HITL 卡片（澄清提问、危险操作审批）
  - 错误消息
- 右侧任务列表（= `session.list`，任务切换、工作目录快捷入口）
- 工作空间侧边栏（按需打开，目录树 + 文件列表内嵌展示）
- 底部输入区
  - 多行文本输入（支持 markdown）
  - 附件按钮与预览区（图片、文件、拖拽）
  - 发送 / 停止合一按钮（按专家状态切换形态）
  - 排队消息提示
  - 模型选择下拉
  - 工作目录选择下拉（见 §2.3）
  - 斜杠命令补全（常用 5 个：/new、/clear、/help、/skills、/model）
  - 文件路径补全（@file: 引用）
  - token 估算
  - 快捷键提示

### 3.2 MVP 不包含

- MoA 参考模型输出
- 内嵌终端实时输出
- 动态 profile 切换（切换需新建 session）
- 高级斜杠命令（仅暴露 5 个常用命令，其余放 v1.1）
- sudo 密码请求、凭据捕获（放 v1.1）
- 后台任务回执（放 v1.1）
- 上下文压缩提示（放 v1.1）
- 子代理开窗观看的实时流（MVP 只做折叠详情）
- 自我改进摘要
- 通知中心历史

## 4. 核心概念映射

| 产品概念 | Hermes 概念 | MVP 实现方式 |
|---|---|---|
| 任务（产品） | Hermes session | `session.list` / `session.create` / `session.resume` |
| 工作空间根 | profile `terminal.cwd` | 专家级目录树根、共享文件；侧边栏展示范围 |
| 工作目录 | session `cwd` | 该任务 workspace；读写 + terminal 锚点 |
| 专家回复 | `message.start` / `message.delta` / `message.complete` | 流式渲染 |
| 思考过程 | `reasoning.delta` / `reasoning.available` | 可折叠区，默认收起 |
| 处理进度 | `status.update`（kind=process/goal） | 气泡下方状态行 |
| 工具调用 | `tool.start` / `tool.progress` / `tool.complete` | 独立卡片 |
| 子代理委派 | `subagent.spawn_requested` / `subagent.start` / `subagent.complete` | 启动 + 完成两块卡片 |
| 澄清提问 | `clarify.request` / `clarify.respond` | HITL 卡片，阻塞 |
| 危险操作审批 | `approval.request` / `approval.respond` | HITL 卡片，阻塞 |
| 用户打断 | `session.interrupt` | 系统行提示 |
| 用户选项回应 | `clarify.respond` | 用户气泡内展示选中项 |
| 错误 | `error` | 红色系统行 |

## 5. 信息架构

### 5.1 页面结构

```text
任务对话页面
  顶部栏：返回 + 专家信息 + 发起任务按钮 + 工作空间按钮 + 状态栏
  主区域：
    左侧：对话流
    中间：任务列表侧边栏（常驻）
    右侧：工作空间侧边栏（按需打开，打开时对话主区变窄、任务列表左移）
  底部：输入区（文本 + 附件 + 发送 + 模型 + 工作目录）
```

任务列表常驻右侧，工作空间侧边栏按需打开。打开工作空间侧边栏时，对话主区宽度减小，任务列表左移，工作空间侧边栏出现在任务列表右侧。

### 5.2 页面示意（工作空间侧边栏关闭）

```text
┌──────────────────────────────────────────────────────────────────────┐
│ ←  人机协作专家 (头像)  [模型: gpt-4o]  [发起任务] [工作空间]        │
├──────────────────────────────────────────────────────┬───────────────┤
│                                                      │ 任务列表      │
│  对话流区域                                          │               │
│                                                      │ ▸ 良率根因分析│
│  [用户气泡] 针对最近良率波动...                      │   进行中      │
│                                                      │ ▸ 工艺窗口复盘│
│  [专家气泡]                                          │   已完成      │
│    ▸ 思考过程                                        │ ▸ 设备关联分析│
│    [工具卡片] data_query                             │   空闲        │
│    [工具卡片] vision_analyze                         │               │
│    专家回复文本...                                   │               │
│                                                      │               │
│  [审批卡片] 即将执行: rm -rf ...                     │               │
│                                                      │               │
├──────────────────────────────────────────────────────┴───────────────┤
│ [已选附件缩略图/文件卡片列表]                                       │
│ [📎] 向专家发起任务指令...                              [发送]      │
│                              Ctrl+Enter 发送 · 123 tokens           │
│ [模型: gpt-4o ▾]                       [工作目录: . (工作空间根) ▾] │
└──────────────────────────────────────────────────────────────────────┘
```

### 5.3 页面示意（工作空间侧边栏打开）

```text
┌──────────────────────────────────────────────────────────────────────┐
│ ←  人机协作专家 (头像)  [模型: gpt-4o]  [发起任务] [工作空间]        │
├──────────────────────────────────────┬──────────┬───────────────────┤
│                                      │ 任务列表 │ 工作空间          │
│  对话流区域 (变窄)                   │          │                   │
│                                      │ ▸ 良率.. │ 📁 D:\workspace   │
│  [用户气泡] 针对最近良率波动...      │   进行中 │ ├ 📂 工位8/       │
│                                      │ ▸ 工艺.. │ ★ D:\workspace    │
│    ▸ 思考过程                        │ ▸ 设备.. │ └ 📄 AGENTS.md    │
│    [工具卡片] data_query             │   空闲   │                   │
│    专家回复文本...                   │          ├───────────────────┤
│                                      │          │ [设为当前工作目录] │
├──────────────────────────────────────┴──────────┴───────────────────┤
│ [📎] 向专家发起任务指令...                              [发送]      │
│ [模型: gpt-4o ▾]                       [工作目录: . (工作空间根) ▾] │
└──────────────────────────────────────────────────────────────────────┘
```

## 6. 顶部状态栏

### 6.1 展示内容

| 元素 | 来源 | 说明 |
|---|---|---|
| 返回按钮 | 产品层 | 返回专家详情页（或上一级入口） |
| 专家头像 + 名称 | profile 配置 | 当前对话的 profile |
| 模型 | `session.info` 的 `model` | 当前使用的模型 |
| 工作目录 | `session.info` 的 `cwd` | 当前任务工作区（相对工作空间根；为根时显示 `.`） |
| 运行状态 | `session.info` 的 `running` | 是否正在执行 |
| 发起任务按钮 | 产品层 | 新建 session 并切换至新对话（同 §8.4，不打开看板抽屉） |
| 工作空间按钮 | 产品层 | 打开工作空间侧边栏；定位到当前 session 工作目录（不在工作空间内则回退到工作空间根） |

### 6.2 状态切换

- 专家运行中：运行状态显示「思考中…」，底部输入区切换为「停止」按钮。
- 专家空闲：运行状态隐藏，底部输入区恢复「发送」按钮。
- 错误状态：运行状态显示红色「出错」。

## 7. 对话流区域

对话流是页面的核心区域，按时间顺序展示用户与专家的所有内容块。每个内容块对应一个或一组 Hermes 事件。

### 7.1 用户消息

#### 7.1.1 纯文本消息

- 蓝底右对齐气泡。
- 支持 markdown 渲染（基础语法）。
- 来源：`prompt.submit` 的 `text`。

#### 7.1.2 图片附件

- 气泡内展示图片缩略图（点击可预览大图）。
- 多张图片横向排列。
- 来源：`input.detect_drop` + `attached_images`。
- 提交前 hermes 会通过 vision 工具预分析，分析描述作为隐藏上下文注入，不在用户气泡里展示。

#### 7.1.3 文件附件

- 气泡内展示文件卡片：文件名、大小、类型图标。
- 来源：`file.attach` RPC → 服务端落地到 session 工作目录 → 返回 `@file:<相对路径>` 引用插入输入框 → 发送时走 `@file:` 注入流程（见 §10.9）。
- 用户可移除未发送的附件。

#### 7.1.4 选项回应（clarify 回应）

- 当用户对专家的澄清提问做出选择后，用户气泡内显示「已选择：XX」。
- 来源：`clarify.respond` 的 `answer`。
- 不重复展示问题原文（问题在专家侧 HITL 卡片里）。

#### 7.1.5 打断

- 用户点击「停止」后，对话流末尾追加一行系统消息：「用户已打断当前任务」。
- 来源：`session.interrupt`。
- 专家正在生成的气泡标记为「已打断」，保留已生成内容。

### 7.2 专家消息

专家侧内容是左对齐的多模块结构化展示，一个专家回合可能包含多个内容块。

#### 7.2.1 思考过程（可折叠）

- 默认收起，标题「思考过程」。
- 展开后显示推理内容文本，支持增量渲染。
- 来源：`reasoning.delta` / `reasoning.available`。
- 一个专家回合内思考过程合并为一个折叠区。

#### 7.2.2 流式回复

- 专家最终回复，左对齐气泡。
- 流式渲染：逐字显示，支持 markdown。
- 来源：`message.start` → `message.delta` → `message.complete`。
- 完成后可展示 usage 信息（token 消耗，可选）。

#### 7.2.3 处理进度提示

- 长任务中专家告知用户当前在做什么。
- 气泡下方一行状态文本，可多行更新。
- 来源：`status.update`（kind=process/goal）。
- 例如：「正在比选 200 万 vs 500 万像素方案并估算检测耗时...」。

### 7.3 工具调用卡片

#### 7.3.1 卡片粒度

- 每个工具调用独立成卡。
- 按时间顺序排列在专家回合内部。
- 卡片之间可折叠（默认展开，用户可一键收起全部工具卡片）。

#### 7.3.2 卡片状态

| 状态 | 来源事件 | 展示内容 |
|---|---|---|
| 进行中 | `tool.start` | 工具名、参数预览、加载动画 |
| 进度更新 | `tool.progress` | 工具名、进度文本（卡片内更新） |
| 完成 | `tool.complete` | 工具名、结果摘要（`summary`）、耗时（`duration_s`） |
| 错误 | `tool.complete` 的 `error` | 工具名、错误信息（红色） |

#### 7.3.3 卡片样式

```text
┌─────────────────────────────────────────┐
│ 🔧 data_query          耗时 2.3s  [▾]   │
│ 参数: table=yield_log                   │
│ 结果: 查询到 100 条记录，良率均值 94.2%  │
└─────────────────────────────────────────┘
```

- 卡片头部：工具图标 + 工具名 + 耗时 + 折叠按钮。
- 卡片体：参数预览（可折叠）+ 结果摘要。
- 默认展示摘要，点击展开查看完整参数和结果。

#### 7.3.4 特殊工具结果

MVP 暂不对 `inline_diff`（文件编辑差异）和 `todos`（待办清单）做特殊呈现，统一作为结果摘要展示。完整差异和待办清单放 v1.1。

### 7.4 子代理卡片

#### 7.4.1 主对话流呈现

主对话流只展示两块卡片：

**启动卡片：**

```text
┌─────────────────────────────────────────┐
│ 🤖 委派子代理: 良率数据深度分析        │
│ 目标: 分析近 30 天良率波动根因          │
└─────────────────────────────────────────┘
```

来源：`subagent.spawn_requested` + `subagent.start`。

**完成卡片：**

```text
┌─────────────────────────────────────────┐
│ ✅ 子代理完成: 良率数据深度分析         │
│ 耗时 45s                                │
│ 摘要: 发现 3 个异常时段，主要集中在...  │
│ [查看详情]                              │
└─────────────────────────────────────────┘
```

来源：`subagent.complete`（`summary`、`duration_seconds`）。

#### 7.4.2 活动详情折叠

- 启动卡片与完成卡片之间的子代理活动（`subagent.thinking` / `subagent.tool` / `subagent.progress`）默认折叠。
- 用户点击「查看详情」展开折叠区，显示活动时间线。
- MVP 不做开窗观看的实时流（放 v1.1）。

### 7.5 HITL 卡片

HITL 卡片阻塞对话流：专家暂停执行，用户必须回应才能继续。

#### 7.5.1 澄清提问（clarify）

```text
┌─────────────────────────────────────────┐
│ ❓ 专家需要澄清                        │
│ 请提供检测缺陷类型、视野范围和当前工位节拍 │
│ [选项 1] [选项 2] [选项 3]             │
└─────────────────────────────────────────┘
```

- 来源：`clarify.request`（`question`、`choices[]`、`request_id`）。
- 用户点击选项后，卡片变为已回答态，选项回应作为用户气泡出现在对话流中。
- 回应 RPC：`clarify.respond`（`answer`、`request_id`）。
- 若 `choices` 为 null，则展示文本输入框。

#### 7.5.2 危险操作审批（approval）

```text
┌─────────────────────────────────────────┐
│ ⚠️ 危险操作待审批                      │
│ 即将执行: rm -rf D:\temp\legacy_logs   │
│ 说明: 清理过期日志目录                 │
│ [拒绝] [批准] [永久放行]               │
└─────────────────────────────────────────┘
```

- 来源：`approval.request`（`command`、`description`、`allow_permanent`）。
- `command` 字段已脱敏（hermes 自动处理）。
- 回应 RPC：`approval.respond`（`choice`：approve/deny/allow_permanent）。
- 用户拒绝后，专家收到拒绝信号并调整策略。
- 用户批准后，专家继续执行。
- 永久放行将命令加入 allowlist，后续相同命令不再询问。

### 7.6 错误消息

- 红色系统行，居中或左对齐。
- 来源：`error` 事件（`message`）。
- 例如：「专家初始化失败：API key 无效」。
- 错误后专家气泡标记为失败态，用户可重试。

## 8. 右侧任务列表（= Session 列表）

### 8.1 定位

右侧列表展示**当前专家 profile 下的 Hermes session**，用于快速切换对话上下文。**不依赖 Kanban**；数据来自 `session.list`（可按 profile 过滤，实现层绑定当前 `HERMES_HOME` / `state.db`）。

### 8.2 数据来源与展示

**列表 RPC：** `session.list` → `sessions[]`，每项至少包含：

| 字段 | 用途 |
|---|---|
| `id` | session 标识；切换时传给 `session.resume` |
| `title` | 任务卡片标题（空则回退为「未命名任务」或首条用户消息摘要） |
| `preview` | 可选副标题/最近一句预览 |
| `last_activity_at` | 最近活跃时间展示（见下） |
| `started_at` | session 创建时间；`message_count === 0` 时作为 `last_activity_at` 回退 |
| `message_count` | 可选，用于区分空会话 |

**最近活跃时间（`last_activity_at`）**：该 session 内**用户最近一条消息**的时间戳；若尚未发送任何用户消息（新建任务、`message_count === 0`），回退为 `started_at`（创建时间）。

**列表排序**：默认按 `last_activity_at` **降序**（最近活跃的任务排在最前）。

每个任务卡片展示：

- 任务标题（`title`）
- 任务状态（产品层由 session 推导，见下表）
- 最近活跃时间（`last_activity_at`，展示为相对或绝对时间，如「刚刚」「10 分钟前」「2026-06-11 06:09」）
- 工作目录按钮（打开工作空间侧边栏，定位到该 session 的 `cwd`）
- 当前激活 session 高亮

**状态映射（MVP，无 Kanban）：**

| 产品状态 | 推导规则 |
|---|---|
| 进行中 | 当前 session 且 `session.info.running === true` |
| 待回应 | 当前 session 存在未回应的 HITL（clarify/approval） |
| 空闲 | 有历史、`running === false`、无阻塞 HITL |
| 未开始 | `message_count === 0`（仅创建、尚未发送首条消息） |

> 若后续接入看板，可在卡片上叠加 Kanban 状态（阻塞/排队等），**不替换**上述 session 列表机制。

### 8.3 切换任务

- 点击任务卡片 → `session.resume`（`session_id` = 列表项 `id`）。
- 切换 = 切换 Hermes session，对话上下文完全隔离。
- 切换前若有未发送的输入，提示用户保存或丢弃。

### 8.4 新建任务

- 任务列表底部「+ 新建任务」与顶部「发起任务」行为一致：**直接新建 session**，不打开看板下发抽屉。
- **RPC：** `session.create`，参数示例：
  - `profile`：当前专家 slug（远程/多 profile 模式）
  - `cwd`：默认 **profile 工作空间根**（`terminal.cwd`）；用户若在 UI 中预先选了目录则传所选路径
  - `title`：可选，首条消息发送后可由系统自动补全
  - `source`：产品层固定值（如 `collab`），便于与 CLI/gateway 会话区分
- **工作目录默认策略：**
  1. **默认** = 工作空间根（与 Hermes `session.create` 未传 `cwd` 时解析 `terminal.cwd` 一致）。
  2. **用户指定**：创建前可在轻量弹层中选工作目录（工作空间内点选）；或创建后在 §9 / §10.7 修改。
  3. **不自动** `mkdir tasks/<session-id>/`；若用户需要隔离，自行选子目录或后续在 SOUL 中约定命名。
- 创建成功后 `session.resume` 到新 session，对话流为空，焦点在输入框。
- 产物约定见 §2.3.5。

## 9. 工作空间侧边栏

### 9.1 定位

工作空间侧边栏展示当前 profile 的**工作空间根**（`terminal.cwd`）完整目录树，用于浏览文件、查看各任务工作目录及其中产物。按需打开，不常驻。

**工作目录**（session `cwd`）是该任务在树中的文件夹节点，即 Hermes 执行锚点；**产物**须落在该目录内，见 §2.3.5。

**所有 session 共享同一 profile 工作空间根**：侧边栏始终展示 `terminal.cwd` 下的完整目录树；当前任务工作目录仅为 ★ 高亮节点，**不**把浏览范围限制在工作目录内。

### 9.2 打开方式

两种入口，行为略有不同：

| 入口 | 行为 |
|---|---|
| 顶部栏「工作空间」按钮 | 打开侧边栏，定位到当前 session 工作目录（若不在工作空间内，回退到工作空间根） |
| 任务卡片的「工作目录」按钮 | 打开侧边栏，自动定位到该任务工作目录 |

### 9.3 布局

工作空间侧边栏出现在任务列表右侧，打开时：

- 对话主区宽度减小
- 任务列表左移
- 工作空间侧边栏占据右侧固定宽度（建议 280–320px）

关闭侧边栏时，对话主区和任务列表恢复原宽度。

### 9.4 展示内容

采用「全工作空间目录树 + 内嵌文件列表」的形式：

- 顶部显示工作空间根路径（如 `D:\workspace`），作为面包屑起点。
- 目录树展示工作空间下所有目录，支持展开/折叠。
- **文件内嵌在目录树中**：展开某个目录时，该目录下的文件直接作为子节点显示在目录树里（不再单独开文件列表面板）。
- 当前 session 的**工作目录**自动高亮（★ 标记 + 背景色），并智能展开其祖先链、自动滚动到可视区域。
- 目录树节点支持文件操作快捷菜单（MVP 仅预览，详见 9.6）。

示例结构：

```text
📁 D:\workspace                 ← 工作空间根
├ 📂 工位8/
└ 📄 AGENTS.md
★ 当前工作目录 = 工作空间根（整棵树高亮根节点，或根路径旁 ★）
```

（若用户将工作目录改为 `工位8/` 等子目录，★ 标记该子节点。）

### 9.5 切换工作目录

- 点击目录树中任意目录节点的「设为当前工作目录」按钮（或在节点右键菜单中选择）。
- 调用 `_set_session_cwd` RPC 更新 session `cwd`；**不**修改 profile 级 `terminal.cwd`（工作空间根不变）。
- 新目录建议位于工作空间根之下；选工作空间外路径需「选择其他目录」并二次确认。
- 更新后，顶部状态栏、底部输入区工作目录显示同步刷新，目录树 ★ 高亮转移。
- MVP 不提供工作目录历史记录。
- 切换工作目录后，专家 terminal 与 file 工具的相对路径基准随之切换（Hermes 原生行为）。

### 9.6 文件预览

- 点击目录树中的文件节点，在侧边栏底部或抽屉中展示文件预览（MVP 仅支持文本与图片，二进制显示文件信息）。
- MVP 不支持在侧边栏内编辑文件，编辑走终端/外部工具。

### 9.7 关闭侧边栏

- 侧边栏右上角提供「✕」关闭按钮。
- 再次点击顶部栏「工作空间」按钮可切换关闭。
- 切换任务（点击任务卡片）时，若侧边栏已打开，保持打开并重新定位到新任务工作目录。

## 10. 底部输入区

底部输入区是用户发起对话指令的核心入口。MVP 在「输入框 + 文件上传 + 发送按钮」基础上，补充模型选择、工作目录选择、附件预览区、排队提示、发送/停止合一按钮、斜杠命令补全、文件路径补全、token 估算、快捷键提示。

### 10.1 输入区布局

```text
┌──────────────────────────────────────────────────────────────┐
│ [已选附件缩略图/文件卡片列表]                                │
├──────────────────────────────────────────────────────────────┤
│ [📎] 向专家发起任务指令...                              [发送] │
│                              Ctrl+Enter 发送 · 123 tokens    │
├──────────────────────────────────────────────────────────────┤
│ [模型: gpt-4o ▾]                       [工作目录: . (工作空间根) ▾] │
└──────────────────────────────────────────────────────────────┘
```

输入区从上到下三层：

1. 附件预览区（有附件时显示，无附件时隐藏）
2. 输入框 + 操作按钮 + 状态提示
3. 配置行（模型选择 + 工作目录选择，位于输入框下方）

### 10.2 输入框

- 多行文本输入，支持 markdown 渲染（基础语法）。
- 提示文字：「向专家发起任务指令...」。
- 输入框自适应高度，最小 1 行，最大 6 行，超出后滚动。

### 10.3 附件按钮与预览区

- 附件按钮位于输入框左侧，点击弹出菜单：上传图片、上传文件。
- 支持拖拽文件到输入框自动识别（图片/文件自动区分）。
- 已选附件在输入框上方展示预览区：
  - 图片附件：缩略图 + 尺寸 + token 估算，点击可预览大图。
  - 文件附件：文件名 + 大小 + 类型图标。
  - 每个附件右上角提供移除按钮（×）。
- 无附件时预览区隐藏，不占用空间。

### 10.4 发送 / 停止按钮（合一）

发送和停止合并为同一个按钮，按专家状态切换形态：

- **专家空闲**：按钮显示「发送」，点击触发 `prompt.submit`。
- **专家运行中**：按钮切换为「停止」，点击触发 `session.interrupt`。
- **HITL 阻塞期间**：按钮显示「停止」并保持可用（可取消整个 HITL），此时不能发送新消息。
- **输入框为空且专家空闲**：按钮禁用。
- 按钮位于输入框右侧，状态切换时无需用户移动鼠标，点击位置不变。

### 10.5 排队消息

- 专家运行中用户继续输入并发送时，由于按钮已切换为「停止」，发送动作改为：点击停止按钮旁边的「排队发送」次按钮，或按 Ctrl+Enter 触发排队。
- 输入框下方提示「消息已排队，将在当前任务后执行」。
- 多条排队消息时显示数量：「3 条消息已排队」。
- 排队消息在当前专家回合结束后自动提交。
- 用户可点击排队提示展开查看排队消息列表，支持移除单条排队消息。

### 10.6 模型选择

- 位于输入框下方配置行左侧。
- 来源：`session.info` 的 `model` 字段。
- 下拉项：当前 profile 可用的模型列表。
- 切换模型后下一个回合生效，不破坏当前回合。
- 切换模型对应 hermes 的 per-session `model_override` 机制。
- 模型名旁可标注是否支持推理（reasoning）能力，便于用户判断。

### 10.7 工作目录选择

- 位于输入框下方配置行右侧。
- 来源：`session.info` 的 `cwd`（**该任务的 workspace**，对齐 Hermes session `cwd`）。
- 展示当前工作目录（相对工作空间根的路径；为根时显示 `.` 或「工作空间根」）。
- 点击下拉提供两个入口：
  - 「在工作空间中选择」：打开工作空间侧边栏（见第 9 节），点选目录设为工作目录。
  - 「选择其他目录」：系统目录选择器（工作空间外需二次确认，不推荐）。
- MVP 不提供工作目录历史记录。
- 切换工作目录后，terminal、file 工具相对路径、上下文文件发现均相对新目录（Hermes 原生）；SOUL 约定任务产物须写在本目录内。
- 工业场景下用户可为不同任务指定不同工作目录（如不同工位/子项目文件夹）。
- 切换后顶部状态栏、工作空间侧边栏 ★ 高亮同步刷新。

### 10.8 斜杠命令补全

- 用户输入 `/` 时弹出命令列表浮层。
- 来源：`complete.slash` RPC。
- 浮层展示命令名 + 简短描述，支持模糊搜索。
- MVP 暴露常用命令：`/new`（新建会话）、`/clear`（清空上下文）、`/help`（帮助）、`/skills`（技能列表）、`/model`（切换模型）。
- 其余高级命令（`/cron`、`/skin`、`/webhook` 等）不在 MVP 暴露，避免 B 端用户混淆。
- 选中命令后自动插入到输入框，用户补充参数后发送。
- 命令发送走 `slash.exec` RPC，结果作为对话内容呈现。

### 10.9 文件路径补全与 `@file:` 上下文引用

`@file:` 是 Hermes 已有的**上下文引用（context reference）**机制，不是单独的工具调用。用户在消息中写 `@file:路径`，发送时 Hermes 自动读取文件内容并注入到消息文本中，agent 收到的已经是包含文件内容的完整消息，不需要额外调工具。

#### 10.9.1 支持的引用类型

| 语法 | 含义 | 示例 |
|---|---|---|
| `@file:path` | 引用文件内容（文本） | `@file:src/main.py` |
| `@file:`path`:10-20` | 引用文件指定行范围 | `@file:`src/main.py`:10-20` |
| `@file:path:10-20` | 同上（无引号） | `@file:src/main.py:10-20` |
| `@folder:path` | 引用目录树结构 | `@folder:src/` |
| `@diff` | 引用 `git diff` 输出 | `@diff` |
| `@staged` | 引用 `git diff --staged` | `@staged` |
| `@git:N` | 引用最近 N 条 `git log -p` | `@git:3` |
| `@url:https://...` | 引用网页内容（走 `web_extract`） | `@url:https://example.com` |

#### 10.9.2 补全（输入时）

- 用户输入 `@file:` 时，前端调 `complete.path` RPC 获取候选文件列表。
- 补全 root = **session cwd**（`_completion_cwd`，见 §10.9.4）。
- 两种匹配模式：
  - **裸名模糊搜索**（如 `@appChrome`）：调 `git ls-files` 列出 repo 内所有文件，按 basename 模糊匹配，返回 top 30。
  - **路径导航**（如 `@file:src/`）：列目录内容，按前缀过滤。
- 选中后插入 `@file:<相对路径>` 到输入框。

#### 10.9.3 内容注入（发送时）

用户点发送后，`prompt.submit` handler 自动处理 `@file:` 标记，产品层无需干预：

1. **正则解析**：从消息文本提取所有 `@file:` / `@folder:` / `@diff` 等引用。
2. **路径解析**：相对路径相对 **session cwd** 解析；`allowed_root` = session cwd。
3. **安全检查**：拦截敏感文件（`.env`、`.ssh/id_rsa`、`.aws/` 等）。
4. **文件读取**：文本文件读取内容，用代码围栏包裹；二进制文件返回路径+类型+大小，引导 agent 用工具处理。
5. **token 预算**：注入总量超 context length 的 25% 软警告，超 50% 硬拒绝（不发消息）。
6. **消息重组**：`@file:` 标记从用户文本中**移除**，文件内容追加到消息末尾的 `--- Attached Context ---` 区块。

最终 agent 收到的消息结构：

```text
用户原始文本（@file:xxx 已移除）

--- Attached Context ---

📄 @file:src/main.py (120 tokens)
```python
def main():
    ...
```
```

#### 10.9.4 `allowed_root` 限制（重要）

`@file:` 的 `allowed_root` = **session cwd**，这是硬限制：

```python
# tui_gateway/server.py prompt.submit 中
ctx = preprocess_context_references(
    prompt,
    cwd=session_cwd,          # 路径解析基准
    allowed_root=session_cwd,  # 硬限制：文件必须在此目录下
    context_length=ctx_len,
)
```

解析后的路径必须 `relative_to(allowed_root)` 成功，否则报 "path is outside the allowed workspace"。

| session cwd | `@file:` 能引用的范围 | 示例 |
|---|---|---|
| = 工作空间根（`terminal.cwd`） | 整棵工作空间树 | `@file:SOP.md` ✅ |
| = `工位8/`（子目录） | 仅 `工位8/` 目录内 | `@file:../SOP.md`（根下文件）❌ 被拦 |
| = 子目录，用绝对路径 | 越界 | `@file:D:\workspace\SOP.md` ❌ 被拦 |

**注意**：agent 的 `read_file` 工具**没有** `allowed_root` 限制，绝对路径直接可读。`@file:` 被拦不代表 agent 读不了该文件--用户可在消息中写路径让 agent 用 `read_file` 读。

#### 10.9.5 子目录 cwd 下引用外部文件的替代方案

当 session cwd 为子目录时，要引用工作空间内其他文件：

| 方式 | 可行性 | 说明 |
|---|---|---|
| 消息里写路径，agent 用 `read_file` 读 | ✅ MVP | `read_file` 无 `allowed_root` 限制，绝对路径直接可读 |
| 改 `allowed_root` 扩至工作空间根 | ✅ v1.1 | 修改 `prompt.submit` 传参，使 `@file:` 支持整棵工作空间 |
| **session cwd 默认 = 工作空间根** | ✅ MVP | `allowed_root` = 根，`@file:SOP.md` 直接可用 |

**PRD 选「默认 cwd = 工作空间根」的核心原因之一**：`@file:` 开箱即用，能引用整棵工作空间树，不需要 `../`，不需要改代码，不需要 agent 额外调工具。

#### 10.9.6 远程文件上传（`file.attach`）

桌面端用户拖拽文件时，如果文件只在客户端磁盘上（远程 gateway 场景），走 `file.attach` RPC：

1. 客户端上传 `data_url`（base64 编码的文件 bytes）。
2. 服务端把文件写到 session 工作目录下的 `.hermes/desktop-attachments/` 子目录。
3. 返回 `@file:<相对路径>` 引用文本，前端插入到输入框。
4. 后续发送时走正常的 `@file:` 预处理流程。

#### 10.9.7 产品层职责

| 能力 | 现状 | 产品层要做什么 |
|---|---|---|
| `@file:` 输入补全 | ✅ `complete.path` RPC | 前端输入 `@file:` 时调 RPC，渲染浮层 |
| `@file:` 内容注入 | ✅ `prompt.submit` 自动处理 | 什么都不用做，正常发消息即可 |
| `@folder:` / `@diff` / `@url:` | ✅ 同一套机制 | 同上 |
| 行号范围 | ✅ 支持 | 同上 |
| 二进制文件 | ✅ 返回路径+类型 | 不用做 |
| 远程文件上传 | ✅ `file.attach` RPC | 拖拽文件时调 RPC |
| token 预算控制 | ✅ 25% 软 / 50% 硬 | 不用做 |
| 敏感文件保护 | ✅ 自动拦截 | 不用做 |
| 路径锚定 session cwd | ✅ Hermes 原生 | 不用做 |

#### 10.9.8 v1.1 可选增强

将 `prompt.submit` 中 `allowed_root` 从 session cwd 扩至 profile 工作空间根（`terminal.cwd`），使 cwd 为子目录时 `@file:` 也能引用整棵工作空间：

```python
# v1.1 改法
workspace_root = _profile_configured_cwd(_profile_home(session.get("profile")))
ctx = preprocess_context_references(
    prompt,
    cwd=session_cwd,                    # 路径解析仍相对 cwd
    allowed_root=workspace_root or session_cwd,  # 但允许范围扩至工作空间根
    context_length=ctx_len,
)
```

同理 `complete.path` 的补全 root 也可选扩至工作空间根。

### 10.10 token 估算

- 输入框右下角实时显示当前输入的 token 估算。
- 文本按 ~4 字符/token 估算。
- 图片附件按 `_estimate_image_tokens`（512px tile × 85 tokens/tile）估算，累加到总数。
- 显示格式：「123 tokens」或「1.2k tokens」。
- 超过阈值（如 4000 tokens）时变红提示，提醒用户输入过长。
- B 端用户对成本敏感，此提示有价值。

### 10.11 快捷键提示

- 输入框右下角小字提示快捷键。
- 「Ctrl + Enter 发送」「Shift + Enter 换行」。
- 快捷键提示与 token 估算同行展示，用「·」分隔。

### 10.12 输入区状态切换汇总

主按钮（发送/停止合一）在不同专家状态下的形态：

| 专家状态 | 主按钮形态 | 附件按钮 | 输入框提示 |
|---|---|---|---|
| 空闲 | 「发送」（输入为空时禁用） | 可用 | 「向专家发起任务指令...」 |
| 运行中 | 「停止」 | 可用 | 「向专家发起任务指令...」+ 排队提示 |
| HITL 阻塞 | 「停止」 | 可用 | 「请先回应专家的提问」 |
| 错误 | 「发送」 | 可用 | 「专家出错，请重试」 |

## 11. 交互规则

### 11.1 对话流滚动

- 新消息到达时自动滚动到底部。
- 用户手动上滚时暂停自动滚动，并显示「回到最新」按钮。
- 工具卡片和子代理卡片更新时，若卡片在可视区域内则平滑更新，否则不打扰。

### 11.2 HITL 阻塞规则

- HITL 卡片出现时，输入框切换为「请先回应专家的提问」提示。
- 用户回应前，主按钮切换为「停止」形态（可取消整个 HITL），附件按钮保持可用。
- 多个 HITL 卡片不会同时出现（hermes 串行处理）。

### 11.3 折叠默认态

| 内容块 | 默认态 |
|---|---|
| 思考过程 | 收起 |
| 工具调用卡片 | 展开（仅摘要） |
| 工具调用参数详情 | 收起 |
| 子代理活动详情 | 收起 |
| 子代理启动/完成卡片 | 展开 |

### 11.4 任务切换

- 切换任务前若有未发送输入，弹窗确认：「丢弃当前输入并切换任务？」。
- 切换任务后对话流完全替换为新任务的 session 历史。
- 切换不保留滚动位置（每个 session 独立维护）。
- 工作空间侧边栏若已打开，切换任务后自动重新定位到新任务工作目录。

## 12. 数据与事件映射

### 12.1 用户操作 → Hermes RPC

| 用户操作 | RPC | 参数 |
|---|---|---|
| 发送文本消息 | `prompt.submit` | `text`、`session_id` |
| 上传图片 | `input.detect_drop` | `text`（拖拽路径） |
| 上传文件 | `file.attach` | `path` 或 `data_url`、`name` |
| 移除图片 | `image.detach` | `path` |
| 回应澄清 | `clarify.respond` | `answer`、`request_id` |
| 回应审批 | `approval.respond` | `choice`、`session_id` |
| 打断专家 | `session.interrupt` | `session_id` |
| 切换任务（session） | `session.resume` | `session_id` |
| 新建任务（session） | `session.create` | `profile`、`cwd`（默认工作空间根）、`title`、`source` |
| 加载任务列表 | `session.list` | `limit`（可选） |
| 切换模型 | 设置 `model_override`（session 内） | 模型名 |
| 切换工作目录 | `_set_session_cwd` | 新目录路径 |
| 配置工作空间根 | profile `terminal.cwd`（专家详情页） | 工作空间根路径 |
| 斜杠命令补全 | `complete.slash` | `query`（用户输入的命令前缀） |
| 文件路径补全 | `complete.path` | `query`（用户输入的路径前缀） |
| 执行斜杠命令 | `slash.exec` | 命令文本 |
| 打开工作空间侧边栏 | profile `terminal.cwd` + `session.info.cwd` | — |
| 浏览工作空间目录树 | 文件系统读取（基于工作空间根） | 目录路径 |

### 12.2 Hermes 事件 → UI 内容块

| Hermes 事件 | UI 内容块 |
|---|---|
| `message.start` / `message.delta` / `message.complete` | 专家流式回复气泡 |
| `reasoning.delta` / `reasoning.available` | 思考过程折叠区 |
| `status.update`（process/goal） | 处理进度提示行 |
| `tool.start` | 工具卡片（进行中） |
| `tool.progress` | 工具卡片进度更新 |
| `tool.complete` | 工具卡片（完成/错误） |
| `subagent.spawn_requested` / `subagent.start` | 子代理启动卡片 |
| `subagent.thinking` / `subagent.tool` / `subagent.progress` | 子代理活动详情（折叠） |
| `subagent.complete` | 子代理完成卡片 |
| `clarify.request` | 澄清提问 HITL 卡片 |
| `approval.request` | 危险操作审批 HITL 卡片 |
| `error` | 红色错误系统行 |
| `session.info` | 顶部状态栏更新、工作空间侧边栏高亮定位更新 |

## 13. 视觉与交互建议

### 13.1 配色

- 用户气泡：蓝色背景，白色文字。
- 专家气泡：白色/浅灰背景，深色文字。
- 工具卡片：浅黄底，工具图标 + 工具名。
- 子代理卡片：浅紫底，机器人图标。
- HITL 卡片：澄清用浅蓝底，审批用浅红底，强提醒边框。
- 错误：红色系统行。

### 13.2 间距与层级

- 对话流内容块之间垂直间距 12px。
- 专家回合内工具卡片之间间距 8px。
- 卡片内部 padding 12px。
- 折叠区展开时平滑动画。

### 13.3 响应式

- 桌面端为主，最小宽度 1024px。
- 右侧任务列表宽度固定 280px，可收起。
- 对话流区域自适应剩余宽度。
- 工作空间侧边栏打开时，对话流区域进一步收窄，最小不低于 480px。

## 14. 后续版本规划

### v1.1

- sudo 密码请求、凭据捕获 HITL 卡片
- 高级斜杠命令（/cron、/skin、/webhook 等全部命令）
- 后台任务回执（`background.complete`）
- 上下文压缩提示（`status.update` kind=compacting）
- 工具结果 `inline_diff` 文件差异特殊呈现
- 工具结果 `todos` 待办清单特殊呈现
- 子代理开窗观看实时流
- 历史消息编辑（`truncate_before_user_ordinal`）
- 分支会话（`session.branch`）
- YOLO 模式开关（自动审批）
- 推理强度调节（`reasoning_config.effort`）
- 快捷指令模板（`quick_commands`）
- 工作目录历史记录（最近 10 个，下拉快速切换）
- **`@file:` `allowed_root` 扩至工作空间根**（使 cwd 为子目录时也能 `@file:SOP.md`，见 §10.9.8）
- **可选：写出工作目录外的写操作软拦截**（plugin / gateway）
- 工作空间侧边栏内文件编辑
- 工作空间侧边栏文件搜索

### v2

- MoA 参考模型输出
- 内嵌终端实时输出
- 动态 profile 切换（切换需新建 session）
- 自我改进摘要（`review.summary`）
- 通知中心历史
- 语音输入
- 富文本工具栏（markdown 快捷按钮）
- 工具集开关（`tools.<platform>.enabled/disabled`）
- max_iterations 限制配置

## 15. MVP 验收标准

### 顶部状态栏

- 顶部栏展示专家头像、名称、模型、工作目录（相对工作空间根）。
- 专家运行时显示「思考中…」状态。
- 错误时显示红色出错状态。
- 「发起任务」按钮直接新建 session 并进入新对话（不打开看板抽屉）。

### 用户消息

- 用户可以发送纯文本消息。
- 用户可以上传图片附件，气泡内展示缩略图。
- 用户可以上传文件附件，气泡内展示文件卡片。
- 用户可以拖拽文件到输入框上传。
- 用户回应澄清提问后，气泡内展示「已选择：XX」。
- 用户点击「停止」后，对话流末尾出现「用户已打断当前任务」系统行。

### 专家消息

- 专家回复支持流式渲染。
- 思考过程默认收起，点击可展开。
- 处理进度提示在气泡下方实时更新。

### 工具调用卡片

- 每个工具调用独立成卡。
- 卡片展示工具名、参数预览、结果摘要、耗时。
- 工具进行中显示加载动画。
- 工具完成显示结果摘要。
- 工具错误显示红色错误信息。
- 卡片可折叠展开查看完整参数。

### 子代理卡片

- 子代理启动时展示启动卡片（含目标）。
- 子代理完成时展示完成卡片（含摘要、耗时）。
- 启动与完成之间的活动详情默认折叠。
- 点击「查看详情」可展开活动时间线。

### HITL 卡片

- 澄清提问卡片展示问题和选项按钮。
- 用户点击选项后卡片变为已回答态。
- 危险操作审批卡片展示命令（脱敏）、说明、拒绝/批准/永久放行按钮。
- HITL 期间输入框提示「请先回应专家的提问」。
- 用户回应前专家不继续执行。

### 错误处理

- 错误消息以红色系统行展示。
- 错误后专家气泡标记为失败态。

### 右侧任务列表（Session 列表）

- 任务列表来自 `session.list`，展示当前专家 profile 下所有 session；默认按最近活跃时间降序排列。
- 每个任务卡片展示标题、状态（由 session 推导）、最近活跃时间、工作目录按钮。
- 最近活跃时间 = 用户最近一条消息的时间；新建且尚未发言的任务展示创建时间。
- 当前激活 session 高亮。
- 点击任务卡片调用 `session.resume` 切换对话上下文。
- 切换前若有未发送输入，弹窗确认。
- 「+ 新建任务」与「发起任务」调用 `session.create`，默认工作目录为工作空间根；不自动创建 `tasks/<id>/`。
- 点击任务卡片的「工作目录」按钮可打开工作空间侧边栏并定位到该 session 工作目录。

### 工作空间侧边栏

- 顶部栏「工作空间」按钮可打开/关闭工作空间侧边栏。
- 打开时对话主区变窄、任务列表左移，侧边栏出现在任务列表右侧。
- 侧边栏展示整个工作空间的目录树，文件内嵌在目录树中。
- 当前 session 的工作目录自动高亮（★ 标记）并智能展开祖先链、滚动到可视区。
- 通过任务卡片「工作目录」按钮打开时，自动定位到该任务工作目录。
- 点击目录节点的「设为当前工作目录」可切换 session 工作目录，顶部状态栏与输入区同步刷新。
- SOUL/skill 包含产物须落在工作目录内的约定；agent 偶发写出工作目录外不视为 MVP 阻塞缺陷。
- 点击文件节点可预览文本/图片文件。
- 侧边栏可关闭，关闭后对话主区与任务列表恢复原宽度。

### 底部输入区

- 输入框支持多行文本和 markdown，自适应高度（1-6 行）。
- 附件按钮可上传图片和文件，支持拖拽。
- 已选附件在输入框上方展示预览区，可逐个移除。
- 发送和停止为同一按钮：专家空闲时显示「发送」，运行中切换为「停止」。
- 输入框为空且专家空闲时按钮禁用。
- Ctrl + Enter 快捷键发送，Shift + Enter 换行。
- 专家运行中通过 Ctrl+Enter 或次按钮触发排队发送，输入框下方显示排队提示和数量。
- 模型选择下拉位于输入框下方，可切换模型，下一回合生效。
- 工作目录选择下拉位于输入框下方，可切换工作目录，支持在工作空间内点选或选择其他目录。
- 输入 `/` 弹出斜杠命令补全浮层，包含 5 个常用命令。
- 输入 `@file:` 弹出文件路径补全浮层（`complete.path` RPC），支持模糊搜索；发送时 `@file:` 自动注入文件内容（见 §10.9）。
- 输入框右下角实时显示 token 估算，超阈值变红。
- 输入框右下角显示快捷键提示。
- HITL 阻塞期间主按钮切换为「停止」，输入框提示「请先回应专家的提问」。
- 错误状态输入框提示「专家出错，请重试」。

## 16. 关键决策总结

1. **一任务一 session**：产品「任务」= Hermes session；列表 = `session.list`，新建 = `session.create`，切换 = `session.resume`；MVP 不依赖 Kanban。
2. **不做技能调用卡片**：Hermes 运行时无此事件，技能列表在顶部状态栏展示。
3. **工具调用独立成卡**：每个工具调用一张卡片，按时间顺序排列，可折叠。
4. **HITL 阻塞对话流**：澄清提问和危险操作审批必须用户回应才能继续。
5. **子代理折叠呈现**：主对话流只展示启动 + 完成两块卡片，活动详情折叠。
6. **MVP 不做高级能力**：MoA、内嵌终端、动态 profile 切换、sudo/secret 放后续版本。
7. **对话内容按事件流映射**：每个 UI 内容块对应一类 Hermes 事件，UI 形态由事件类型决定。
8. **输入区三层布局**：附件预览区 + 输入框与按钮 + 配置行（模型/工作目录位于输入框下方）。
9. **发送/停止按钮合一**：同一按钮按专家状态切换形态，点击位置不变，交互连贯。
10. **模型选择放输入框下方**：per-session `model_override`，下一回合生效，无需新建 session。
11. **工作目录 = session `cwd`**：即该任务 workspace，对齐 Hermes 原生；产物须落在工作目录内（SOUL 约定）。
12. **所有 session 共享 profile 工作空间根**：`terminal.cwd` 下整棵树可浏览/可读；默认 session `cwd` 即根；用户改为子目录时**不**缩小侧边栏浏览范围（见 §2.3.4）。
13. **斜杠命令精简暴露**：MVP 仅暴露 5 个常用命令（/new、/clear、/help、/skills、/model），其余放 v1.1。
14. **token 估算实时提示**：输入框右下角显示，超阈值变红，B 端用户对成本敏感。
15. **任务列表常驻、工作空间侧边栏按需**：列表数据来自 `session.list`，按 `last_activity_at`（用户最近发言，空 session 回退 `started_at`）降序；侧边栏树根 = profile 工作空间根；★ 高亮当前 session 工作目录（默认即根）。
16. **工作空间根 vs 工作目录**：profile `terminal.cwd` = 专家级文件树根；session `cwd` 默认 **等于** 工作空间根，用户可改为子目录；**不**自动按 session id 建 `tasks/<id>/`。
17. **目录树+内嵌文件列表**：工作空间侧边栏展示完整目录树，文件内嵌在目录树中。
18. **MVP 不做工作目录历史**：切换不保留历史记录，放 v1.1。
19. **Hermes 内部状态不进工作目录**：sessions DB、memory、config 仍在 `HERMES_HOME`；工作目录指磁盘上的 task 文件夹。
