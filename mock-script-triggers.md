# 模拟对话触发词

任务对话页在 **DEV_MOCK** 模式下（默认 `index.html`，未带 `?mode=sidecar`），发送消息会走 `AppStore.playMockScript`，按输入文本命中剧本。

代码入口：

- 触发表：`js/store.js` → `mockScriptTriggers`
- 匹配：`resolveMockScript(text)`，**按数组顺序，先命中先生效**
- 播放：`playMockScript` → `js/expert-tasks-page.js` 映射到思考 / 工具 / 回复

未命中任何词时走默认剧本 `normal`。

---

## 一览（按匹配优先级）

| 优先级 | 剧本 | 触发词 | 对话形态 | 来源 |
|---|---|---|---|---|
| 1 | `phased` | 分步、分段、中途、先说、边做边说、分阶段、穿插 | 处理 → 中途输出 → 再处理 → 最终回复 | 本次新增 |
| 2 | `loop` | 反复、交替、再想、多轮、边想边调、继续查 | 思考 → 工具 → 再思考 → 再工具 → 回复 | 本次新增 |
| 3 | `error` | 错误、失败、报错、异常 | 思考 → 工具失败 → 错误行 → 回复 | 原有 |
| 4 | `thought` | 思考、think、推理、思路 | 仅思考 → 回复 | 原有 |
| 5 | `tool` | 调用工具、工具、查询、检索、tool | 思考 → 多次工具 → 回复 | 原有（后扩成多工具） |
| 6 | `subagent` | 子智能体、复杂、委派、subagent | 思考 → 子智能体 → 回复 | 原有 |
| 7 | `clarify` | 澄清、clarify | 处理 → 说明 → 澄清卡片 | 本次补过程 |
| 8 | `approval` | 审批、确认、approval | 处理 → 说明 → 审批卡片 | 本次补过程 |
| — | `normal` | 无触发词 | 思考 → 少量工具 → 一次回复 | 原有（后补了工具） |

句子里只要含有上表词语即可，不必整句等于触发词。例如「请分步评估工位8」会走 `phased`。

---

## 各剧本说明

### 1. `phased` 分阶段输出

**触发词：** 分步、分段、中途、先说、边做边说、分阶段、穿插

**流程：**

```
处理中（思考 + search_files + read_file）
↓ 收起
中途回复
↓
处理中（思考 + terminal + 专家工具）
↓ 收起
最终回复
```

**示例：** `请分步评估工位8视觉检测方案`

**历史任务：** 人机协作专家 →「分阶段分析工位8节拍」

---

### 2. `loop` 过程内多轮思考

**触发词：** 反复、交替、再想、多轮、边想边调、继续查

**流程：** 同一条「处理完成」里交替出现思考和工具，全部结束后才回复。

```
思考 → search_files
→ 再思考 → read_file + terminal
→ 再思考 → 专家工具
→ 回复
```

**示例：** `请反复核对工位8节拍数据`

**历史任务：** 人机协作专家 →「多轮核对工位8节拍数据」

> 「再思考」「多轮思考」会命中本条（`再想` / `多轮` 排在 `思考` 之前），不会走进纯思考剧本。

---

### 3. `error` 工具失败

**触发词：** 错误、失败、报错、异常

**流程：** 思考 → 调用专家工具失败 → 对话错误行 → 抱歉回复

**示例：** `查询失败了怎么办`

---

### 4. `thought` 仅思考

**触发词：** 思考、think、推理、思路

**流程：** 流式思考 → 一句「已完成思考，准备开始执行。」

**示例：** `先思考一下这个问题`

---

### 5. `tool` 多次工具调用

**触发词：** 调用工具、工具、查询、检索、tool

**流程：**

```
思考
→ search_files
→ skill_view（失败）
→ read_file
→ terminal
→ 专家工具
→ 回复
```

**示例：** `请检索工位8相关资料`

---

### 6. `subagent` 子智能体

**触发词：** 子智能体、复杂、委派、subagent

**流程：** 思考 → 委派 `research-sub-agent`（内部含思考 / 工具 / 回复）→ 主回复

**示例：** `这个问题比较复杂，委派子智能体处理`

---

### 7. `clarify` 澄清

**触发词：** 澄清、clarify

**流程：**

```
处理中（思考 + search_files + read_file）
↓ 收起
说明回复（需要选择分析维度）
↓
澄清卡片（按时间趋势 / 按地域分布 / 按产品类别）
```

用户选择后：再调一次工具 → 最终回复。

**示例：** `需要澄清一下分析维度`

---

### 8. `approval` 审批

**触发词：** 审批、确认、approval

**流程：**

```
处理中（思考 + search_files + 专家工具）
↓ 收起
说明回复（已聚合结果，导出需确认）
↓
审批卡片（允许 / 允许并记住 / 拒绝）
```

通过后写入工作空间并回复；拒绝则回复已取消。

**示例：** `执行前请先审批`

---

### 9. `normal` 默认

**触发词：** 无

**流程：** 思考 → search_files → 专家工具 → 一次回复

**示例：** `工位8节拍偏高，帮我看看`

---

## 容易撞词的情况

匹配从上到下，**只取第一条**。

| 输入 | 实际剧本 | 原因 |
|---|---|---|
| 请分步查询 MES | `phased` | `分步` 优先于 `查询` |
| 请反复检索资料 | `loop` | `反复` 优先于 `检索` |
| 查询失败了 | `error` | `失败` 优先于 `查询` |
| 再思考一遍 | `loop` | `再想` 优先于 `思考` |
| 先思考再查询 | `thought` | `思考` 优先于 `查询` |
| 复杂查询 | `tool` | `查询` 优先于 `复杂`（`复杂` 只在 subagent） |

若要走指定剧本，避免同时写更高优先级的词。

---

## 历史演示任务（人机协作专家）

| 任务 | 覆盖 |
|---|---|
| 阶段功能演示 · 内容块全景 | 长工具链、失败、子智能体、HITL、分阶段、过程内多轮，文末附触发词列表 |
| 分阶段分析工位8节拍 | `phased` 静态样例 |
| 多轮核对工位8节拍数据 | `loop` 静态样例 |

改剧本后需递增 `js/demo-data.js` 中的 `DEMO_DATA_VERSION`，刷新后才会重同步本地演示数据。

---

## 相关但不是模拟剧本

| 能力 | 写法 | 说明 |
|---|---|---|
| 引用工作空间文件 | `@file:路径` | 输入区补全，发送时注入文件内容，不走上述剧本 |

模拟开关：`js/demo-config.js`。`?mode=sidecar` 时走真实 sidecar，触发词不生效。
